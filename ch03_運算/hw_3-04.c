#include <stdio.h>
int main(){
	int i = 0;
	scanf("%d", &i);
	printf("%d\n", ((i%3 == 0)&&(i%5 != 0)) || (i%3 != 0)&&(i%5 == 0));

	return 0;
}


/*
寫一個程式讀入一個正整數。
如果該整數為3的倍數但不為5的倍數則輸出1，或是為5的倍數但不為3的倍數則輸出1，
其他狀況均輸出0。
*/
